---
# ignoring nvidia and tplink for now
exclude_paths:
  - requirements.yml
  - .github
  - roles/nvidia
  - roles/tplink
skip_list:
  # we do not generate roles with ansible-galaxy
  - meta-no-info
